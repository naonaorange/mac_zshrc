% how to export additional path to env path
%export PATH=$PATH:XXXXX

alias ls='ls -GF'
alias ll='ls -l'

% change user name and host name in terminal
export PS1="%~ $"

% brew
export PATH=$PATH:/opt/homebrew/bin

% python
alias python='python3.9'
